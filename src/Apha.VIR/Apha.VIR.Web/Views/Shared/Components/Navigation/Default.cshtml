@model List<NavItem>

@if (Model?.Any() == true)
{
    <ol class="breadcrumb">
        @for (int i = 0; i < Model.Count; i++)
        {
            var item = Model[i];

            if (i < Model.Count - 1)
            {
                <li class="breadcrumb-item">
                    <a title="@item.Description" href="@item.Url">@item.Title</a>
                </li>
            }
            else
            {
                <li class="breadcrumb-item active" aria-current="page">@item.Title</li>
            }
        }
    </ol>
}
