@model AuditLogViewModel
@{
    ViewData["Title"] = "Search Audit Log | Virus Isolates Repository";
}
<div class="isolatereport-contentbox">
    @* <div class="card"> *@
    <div class="isolatereport-box_title">
            Enter search selection criteria
        </div>
    <div class="isolatereport-box_body">
        <form asp-action="SearchAudit" method="post">
                <div class="form-group row">
                    <label asp-for="AVNumber" class="col-sm-3 col-form-label">AVNumber</label>
                    <div class="col-sm-9">
                        <input asp-for="AVNumber" class="form-control" placeholder="AVnnnnnn-yy or PDnnnn-yy" />
                        <span asp-validation-for="AVNumber" class="text-danger"></span>
                    </div>
                </div>
                <div class="form-group row">
                    <label asp-for="DateTimeFrom" class="col-sm-3 col-form-label">Date/Time From</label>
                    <div class="col-sm-9">
                        <input asp-for="DateTimeFrom" class="form-control datepicker" placeholder="dd/mm/yyyy hh:mm:ss" />
                        <span asp-validation-for="DateTimeFrom" class="text-danger"></span>
                    </div>
                </div>
                <div class="form-group row">
                    <label asp-for="DateTimeTo" class="col-sm-3 col-form-label">Date/Time To</label>
                    <div class="col-sm-9">
                        <input asp-for="DateTimeTo" class="form-control datepicker" placeholder="dd/mm/yyyy hh:mm:ss" />
                        <span asp-validation-for="DateTimeTo" class="text-danger"></span>
                    </div>
                </div>
                <div class="form-group row">
                    <label asp-for="UserId" class="col-sm-3 col-form-label">User</label>
                    <div class="col-sm-9">
                        <input asp-for="UserId" class="form-control"  />
                        <span asp-validation-for="UserId" class="text-danger"></span>
                    </div>
                </div>
                <div class="form-group row">
                    <div class="col-sm-9 offset-sm-3">
                        <button type="submit" class="btn btn-primary">Search</button>
                        <a asp-action="ClearSearch" class="btn btn-secondary">Clear Search</a>
                    </div>
                </div>
            </form>
        </div>
   @*  </div> *@
</div>
    <div id="searchResults">
      @*   @if (!string.IsNullOrEmpty(Model.ValidationError))
        {
            <div class="alert alert-danger" role="alert">
                @Model.ValidationError
            </div>
        } *@

       @*  @if (Model.ShowResults)
        { *@
            <h3>Select the audit log that you wish to view</h3>
            <div class="btn-group" role="group" aria-label="Audit Log Types">
                <button type="button" class="btn btn-secondary" id="btnSubmission">Submission</button>
                <button type="button" class="btn btn-secondary" id="btnSample">Sample</button>
                <button type="button" class="btn btn-secondary" id="btnIsolate">Isolate</button>
                <button type="button" class="btn btn-secondary" id="btnCharacteristics">Characteristics</button>
                <button type="button" class="btn btn-secondary" id="btnDispatch">Dispatch</button>
                <button type="button" class="btn btn-secondary" id="btnIsolateViability">Isolate Viability</button>
            </div>

            <div id="auditLogContent" class="mt-3">
                <!-- Content for each audit log type will be loaded here via AJAX -->
            </div>
       @*  } *@
    </div>
