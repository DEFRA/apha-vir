@model LookupItemListViewModel

@if (Model != null && Model.LookupItems.Any())
{
	<table class="isolatereporttable">
		<thead>
			<tr class=".isolatereport-header">
				<th>Name</th>
				@if (Model.HasAlternateName)
				{
					<th>Alternate Name</th>
				}
				@if (Model.HasParent)
				{
					<th>Parent Name</th>
				}
				<th>Active</th>
				@if (Model.IsSMSRelated)
				{
					<th>SMS</th>
					<th>SMSCode</th>
				}
				<th>Edit</th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model.LookupItems)
			{
				<tr>
					<td>@item.Name</td>
					@if (Model.HasAlternateName)
					{
						<td>@item.AlternateName</td>
					}
					@if (Model.HasParent)
					{
						<td>@item.ParentName</td>
					}
					<td>@item.Active</td>
					@if (Model.IsSMSRelated)
					{
						<td>@item.Sms</td>
						<td>@item.Smscode</td>
					}
					<td>
						<a asp-action="Edit" asp-route-lookupid="@item.Id"
						   class="btn btn-primary">Edit</a>
					</td>
				</tr>
			}
		</tbody>
	</table>
	<div id="girdPagination" class="pagination">
		@await Component.InvokeAsync("Pagination", Model.Pagination)
	</div>
}
else
{
	<p>There are no entries recorded for this look-up list.</p>
}